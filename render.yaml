databases:
  - name: airbnbClone
    databaseName: mysite
    user: mysite
    region: singapore

services:
  - type: web
    name: airbnbClone
    env: python
    region: singapore

    buildCommand: "./build.sh"
    startCommand: "gunicorn mysite.wsgi:application"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: airbnbClone
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: WEB_CONCURRENCY
        value: 4